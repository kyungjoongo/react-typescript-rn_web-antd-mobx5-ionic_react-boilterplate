import React, {useEffect} from "react";


export function DatabaseProvider(props: any) {
    useEffect(() => {
    }, [])

    return (
        <div>
            {props.children}
        </div>
    )

}
